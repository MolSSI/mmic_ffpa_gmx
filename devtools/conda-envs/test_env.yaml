name: test
channels:
  - conda-forge
  - defaults
  - bioconda
dependencies:
    # Base depends
  - python
  - pip
  - gromacs

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

